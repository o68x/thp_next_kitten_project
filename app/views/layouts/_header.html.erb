<header class="header-sticky header-dark">
  <div class="container">
    <nav class="navbar navbar-expand-lg navbar-dark">
       <%= link_to 'Kitten Project', root_path, class: "navbar-brand" %>
      <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation"><span class="burger"><span></span></span></button>
      <div class="collapse text-right navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav align-items-center mr-auto">
          <li class="nav-item">
            <%= link_to "About Us", about_path, class: "nav-link"%>
          </li>
          <li class="nav-item">
            <%= link_to "Team Project", team_project_path, class: "nav-link"%>
          </li>
        </ul>
          <ul class="navbar-nav align-items-center mr-0">
            <% if user_signed_in? %>
            <li class="nav-item dropdown">
              <%= link_to "Sign out", destroy_user_session_path, method: :delete, class: "nav-link" %>
            </li>
            <li class="nav-item dropdown">
              <%= link_to "Profile", edit_profile_path(current_user.id), class: "nav-link" %>
            </li>
            <li class="nav-item dropdown">


              <a id="drop_cart_btn" class="nav-link, btn btn-outline-primary btn-sm" style= " width: fit-content;
                                                                                              margin-left: auto;
                                                                                              background: #fff0" >
                <!-- #TODO: CSS Get color right on scroll -->
                <span >  <%= image_tag "caddie.png", alt: "Carts", class: " ", id:"drop_cart_btn", style:"width: 35px"  %>&nbsp;</span>
                <small id="drop_car_badge" class="badge badge-pill badge-info">
                  <%= current_cart.count_cats %>
                </small>
              </a>

            </li>
        <% else %>
            <li class="nav-item dropdown">
              <%= link_to "Log in", new_user_session_path, class: "nav-link" %>
            </li>
            <li class="nav-item dropdown">
              <%= link_to "Sign in", new_user_registration_path, class: "nav-link" %>
            </li>
        <% end %>
        </ul>
      </div>
    </nav>
    <% if user_signed_in? %>
      <%= render 'carts/partials/cart_dropdown' %>
    <% end %>
  </div>
</header>

