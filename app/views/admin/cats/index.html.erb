    <!-- banner -->
    <%= render 'cats/banner' %>
    <!-- /banner -->

    <!-- sidebar -->
    <%= render 'sidebar' %>
    <!-- /sidebar -->

        <main role="main" class="col-md-9 ml-sm-auto col-lg-10 pt-3 px-4">
          <div class="d-flex justify-content-between flex-wrap flex-md-nowrap align-items-center pb-2 mb-3 border-bottom">
            <h1 class="h2">Items Dashboard</h1>
            <div class="btn-toolbar mb-2 mb-md-0">
              <div class="btn-group mr-2">
                <button class="btn btn-sm btn-outline-secondary">Share</button>
                <button class="btn btn-sm btn-outline-secondary">Export</button>
              </div>
              <button class="btn btn-sm btn-outline-secondary dropdown-toggle">
                <span data-feather="calendar"></span>
                This week
              </button>
            </div>
          </div>

          <!--<canvas class="my-4" id="myChart" width="900" height="380"></canvas> -->

          <h2>Cats pictures preview</h2>
            <div class="row">
              <%= link_to "New Item" , new_admin_cat_path, class:"btn btn-with-ico btn-rounded btn-primary" %>
            </div>

              <div class="table-responsive">
                <table class="table table-striped table-sm">
                  <thead>
                    <tr>
                      <th>#</th>
                      <th>Title</th>
                      <th>Price</th>
                      <th>See</th>
                      <th>Edit</th>
                      <th>Delete</th>
                    </tr>
                  </thead>
                  <tbody>
                  <% @cats.each do |cat|%>
                    <tr>
                      <td><%= cat.id%></td>
                      <td><%= cat.title%></td>
                      <td><%= cat.price%></td>
                      <td><span title= "Voir" ><%= link_to '<i class="btn btn-sm btn-ico btn-primary "></i>'.html_safe, cat_path(cat)%></span></td>
                      <td><span title= "Editer" ><%= link_to '<i class="btn btn-sm btn-ico btn-instagram"></i>'.html_safe, edit_admin_cat_path(cat)%></span>
                      <td><span title= "Supprimer" ><%= link_to '<i class="btn btn-sm btn-ico btn-git"></i>'.html_safe, cat_path(cat) , method: "delete", data: { confirm: "Es-tu sûr de vouloir supprimer ce chat ?" } %></span></td></td>
                    </tr>
                  <%end%>
                  </tbody>
                </table>
              </div>
        </main>
      </div>
    </div>
